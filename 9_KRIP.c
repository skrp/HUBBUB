##################################
# KRIP - recursive file encryption
# ARG1 - target path
# ARG2 - dump path
# inclue <stdio.h>

int keygen($key3)
{
  FILE *kfp; // KEY FILE
  
  if (open(argv[1], "r") < 0)
    { printf("ERR read %s\n", argv[1]); exit(1); }
}
